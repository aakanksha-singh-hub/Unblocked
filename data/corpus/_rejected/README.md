# Rejected batch

These replies failed the provenance audit in `eval/provenance.py`: uniform
formal English, zero Hindi tokens in a corpus elicited as Hinglish, and — in an
earlier revision — the same thirty-word sentence appearing verbatim under three
different contributors.

Kept rather than deleted because the audit that rejected them is a real part of
this project, and the material it was written against should be inspectable.

They are not part of any corpus, are excluded from every build, and no number in
this project is computed from them.
